package game.objects;

//This creates an enumeration (ID) for every possible object
public enum ID
{
	Player(),
	Trail(),
	BasicEnemy(),
	FastEnemy(),
	SmartEnemy(),
	EnemyBoss(),
	MenuParticle();
	//Now we can ID something as a player or enemy.
	
}
